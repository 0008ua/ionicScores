import { RoundDirective } from './round.directive';

describe('RoundDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundDirective();
    expect(directive).toBeTruthy();
  });
});
