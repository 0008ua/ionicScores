import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.scores.homegames',
  appName: 'scores',
  webDir: '../public',
  bundledWebRuntime: false
};

export default config;
